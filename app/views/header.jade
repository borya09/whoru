nav
    ul
        li.wh-header-locales
            ul
                li.wh-header-locale(ng-repeat='locale in locales', ng-class='{actual: locale.actual}')
                    a(href, ng-click="setLocale(locale)", title="{{locale.text}}")
                        img(ng-src="{{locale.image}}", title="{{locale.text}}")